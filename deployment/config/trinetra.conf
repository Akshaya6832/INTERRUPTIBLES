# TRINETRA development configuration
# Validator requirements retained from the validated implementation:
MAX_SENSOR_AGE_MS=5000
MAX_FUTURE_TIMESTAMP_MS=1000
WATER_LEVEL_MIN_M=0.0
WATER_LEVEL_MAX_M=20.0
WATER_LEVEL_MAX_RATE_MPM=2.0
# Demo hazard thresholds are compiled defaults in trinetra_config.h.
